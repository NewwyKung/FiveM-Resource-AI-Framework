Follow the repository-wide instructions in `AGENTS.md`.

Before editing, load only the relevant files from `.ai/rules/` and one matching workflow from `.ai/skills/INDEX.md`. Treat `docs/decisions/` as approved architecture. Never edit generated files under `resource/html/`.
