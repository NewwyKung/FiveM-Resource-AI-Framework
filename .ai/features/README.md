# Feature Registry

Use one Markdown file per implemented feature, based on `TEMPLATE.md`.

Create a feature record when work spans multiple runtime folders or introduces contracts, configuration domains, UI screens, exports, events, or persistent data. Keep each record concise and link to source files rather than copying implementations.
